import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.musica_linguae.app',
  appName: 'Musica-Linguae',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
